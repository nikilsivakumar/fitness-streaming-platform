name: 'fitness_streaming_aws'
version: '1.0.0'
config-version: 2

profile: 'fitness_streaming_aws'

model-paths: ["models"]
macro-paths: ["macros"]
test-paths: ["tests"]
seed-paths: ["seeds"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  fitness_streaming_aws:
    staging:
      +materialized: view
    marts:
      +materialized: table
